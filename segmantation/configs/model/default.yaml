_target_: segmantation.model.model.SegModel
image_path: ${params.image_path}
mask_path: ${params.mask_path}
train_ratio: ${params.train_ratio}
num_workers: ${params.num_workers}
batch_size: ${params.batch_size}
net: ${net}
 